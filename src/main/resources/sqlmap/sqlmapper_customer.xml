<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- MyBatis에게 Mapper 경로를 적어야한다. -->
<mapper namespace="com.example.first_spring.mapper.CustomerMapper"><!-- mapper위치 경로-->
<select id="getCustomerList" resultType="CustomerVO">
<!-- 쿼리의 결과값을 CustomerVO로 하겠다. -->
<!-- 결과값을 위 경로에 설정한 CustomerMapper의 메소드(id="getCustomerList")로 return -->
SELECT
*
FROM customer
</select>

</mapper>